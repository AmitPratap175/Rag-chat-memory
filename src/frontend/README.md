# install all the dependencies/module

1. cd src\frontend
2. npm install  (must be run inside the frontend folder)

A folder named node_modules get created in the frontend.

# to add the frontend to backend 

1. npm run build

dist or static folder get created.

# run program after successfully installation of the modules

1. npm run dev (to start the npm server at localhost:5173)